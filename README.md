Login App – React + Spring Boot + MySQL

A simple full-stack login application built using:

React (frontend)

Spring Boot (backend)

MySQL (database)

📌 Features

✔ User login with username & password
✔ Backend validation
✔ REST API with Spring Boot
✔ Clean React UI
✔ MySQL database connection
